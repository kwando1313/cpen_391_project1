#include <stdio.h>
#include <stdlib.h>
#include "graph.h"

int main(void) {
	printf("start\n");

	graph* graph = init_graph(5);
//	vertex v0 = init_vertex(int id, int latitude, int longitude, int altitude, char* name, int x, int y);
//	int add_vertex(graph* graph, vertex v);

	printf("end\n");
	return 0;
}
